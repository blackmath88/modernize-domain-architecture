# Provider Boundary Experiment

WS-02 stores reproducible run records and derived comparison material here. Raw artifacts may live under `evidence/` when large or naturally grouped there; this directory must link them without rewriting them.

Use the A-G matrix in `EXPERIMENT_PLAN.md` and the capture format in `experiments/runbooks/RUN_TEMPLATE.md`.
