# Preserved Source Notes

This directory is a verbatim snapshot of the material supplied before repository synthesis.

- `handoff/` contains the complete original `modernize-domain-architecture-handoff` directory.
- `root-artifacts/` contains the original root HTML, Markdown, and JSON artifacts.

These files are primary research inputs, not the canonical current interpretation. Some claims in visual artifacts use persuasive narrative language; consult the canonical root documents and `structured/claims.json` for current epistemic status.

Known duplication is intentionally preserved:

- `machine-executable-domain-packs.html` and `machine-executable-domain-packs (1).html` are exact duplicates.
- the two Modernize scrollytelling files are design variants; v2 is the preferred visual reference.
- the product-value Markdown and documentation metadata occur in more than one supplied location.